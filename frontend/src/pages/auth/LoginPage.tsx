// ============================================================
// Page 2 — Login (Sign In) Page
// Full-Screen 3D Parallax Glassmorphic Auth Portal
// ============================================================

import { AuthPortalPage } from './AuthPortalPage'

export default function LoginPage() {
  return <AuthPortalPage initialMode="login" />
}
